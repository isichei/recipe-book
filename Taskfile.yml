version: '3'

tasks:
  test:
    - go test ./internal/recipes -v

  start-app:
    deps: [make-templates]
    cmds:
      - go run ./cmd/web

  make-templates:
    - /home/karik/go/bin/templ generate
